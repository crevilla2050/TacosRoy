' Gambas class file


Public Sub Form_Open()

  Dim hConn As Connection
  Dim hResult As Result
  Dim iInd As Integer
  Dim iCol As Integer
  Dim iNrElements As Integer
  Dim tempString As String
  
  'Acomodar los botones
  hConn = modConn.ConectarDB()
  hResult = hConn.Exec("Select * FROM vw_products_catprecio")
  
  DataSource2.Connection = hConn
  'DataSource2.Table = "vw_products_catprecio"
  hResult = Null
  
  hResult = hConn.Exec("Select * FROM tbl_categorias")
  DataSource1.Connection = hConn
  DataSource1.Table = "tbl_categorias"
  
  'DataSource2.Filter = "chr_nombre_cat"
  
  
  'Cierra la conexión
  hConn = Null

End
